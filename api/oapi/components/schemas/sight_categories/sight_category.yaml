components:
  schemas:
    SightCategory:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: integer
          format: int64
          description: id(PK)
          example: 1
        name:
          type: string
          description: 名前
          example: "観光施設"
